import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  gps_icon: {
    width: '20px',
    display: 'flex',
    margin: '0 auto',
  },
  text: {
    lineHeight: 1,
  },
}));
